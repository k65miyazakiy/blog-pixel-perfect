---
title: "Sample Markdown Reference Page"
createdAt: "2030-12-01"
updatedAt: "2030-12-01"
tags:
  - markdown
  - sample
development: true
---

# 見出し 1

## 見出し 2

### 見出し 3

#### 見出し 4

##### 見出し 5

###### 見出し 6

---

これは通常のテキストブロックです。lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.

テキストブロック2です。lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.

**太字**

_斜体_

~~取り消し線~~

これは**太字**で、これは*斜体*で、これは~~取り消し済みのテキスト~~です。

---

---

- 箇条書きリスト
  - ネストされたリスト
    - さらにネスト

1. 番号付きリスト
2. 番号付きリスト
   1. ネストされた番号付きリスト
   2. ネストされた番号付きリスト

---

[リンクテキスト](https://example.com)

---

`日本語のインラインコード`と、`English inline code`です。

```javascript
// コードブロック
function helloWorld() {
  console.log("Hello, world!");
}
```

## カスタムコンポーネント

### テストコンポーネント

<Test />

### 引用

<BQ> これは引用です。</BQ>
<BQ source="aaaa"> これは引用元を含んだ引用です。</BQ>
<BQ source="aaaa" sourceAddr="http://github.com">
  これは引用元およびアドレスを含んだ引用です。
</BQ>
<BQ source="aaaa" sourceAddr="http://github.com" author="John Doe">
  これは引用元およびアドレス, 著者名を含んだ引用です。
</BQ>

### Callouts

#### 情報系

<Info message="これは一般的な情報提供メッセージです。" />

<Note message="これは補足説明や注記用のメッセージです。" />

<Tip message="これは役立つヒントやコツを伝えるメッセージです！" />

#### 注意・警告系

<Warning message="これは注意喚起のメッセージです。操作前に確認してください。" />

<Caution message="これは慎重に扱うべき内容についてのメッセージです。" />

<Danger message="これは危険な操作についての警告メッセージです。十分注意してください。" />

#### 結果系

<Success message="操作が正常に完了しました。" />

<Error message="エラーが発生しました。設定を確認してください。" />

#### 長いメッセージのテスト

<Info message="これは非常に長いメッセージのテストです。画面幅を超える場合の表示確認のため、意図的に長いテキストを記述しています。このメッセージは複数行にわたって表示され、適切に改行処理されることを確認するためのものです。さらに長くするために追加のテキストを含めています。モバイル端末やデスクトップの様々な画面サイズでも適切に表示されることを期待しています。" />

### コードブロック

#### 基本的なコードブロック

<CB
  lang="javascript"
  fileName="sample.js"
  content={`\
  // コードブロック
  function helloWorld() {
    console.log("Hello, world!");
  }`}
/>

#### 長いコードのテスト（横スクロールテスト）

<CB
  lang="javascript"
  fileName="long-code-example.js"
  content={`\
// このコードは意図的に長い行を含んでいます。横スクロールのテストを行うためです。
const veryLongVariableName = "This is a very long string that should trigger horizontal scroll when the screen width is narrow. The purpose is to test how the code block handles long lines of code.";

// 関数の定義も長いパラメータリストでテスト
function processUserDataWithVeryLongParameterNames(userFirstName, userLastName, userEmailAddress, userPhoneNumber, userAddress, userDateOfBirth, userPreferences) {
    // 長いチェーンメソッドでのテスト
    return userFirstName.trim().toLowerCase().replace(/\\s+/g, '-') + '@' + userEmailAddress.split('@')[1].toLowerCase().replace(/[^a-z0-9.-]/g, '') + '.processed';
}

// 長いオブジェクトの定義
const configurationObject = {
    apiEndpoint: "https://api.example.com/v1/users/profile/detailed-information-with-extended-metadata",
    authentication: {
        tokenType: "Bearer",
        refreshTokenEndpoint: "https://auth.example.com/oauth/token/refresh/with-extended-scope"
    },
    requestOptions: {
        timeout: 30000,
        retries: 3,
        headers: {
            "Content-Type": "application/json",
            "User-Agent": "MyApp/1.0 (https://myapp.example.com; contact@myapp.example.com)"
        }
    }
};

// コメントも意図的に長くして、横スクロールが必要な状況を作っています。このような状況でも適切に表示されることを確認します。`}
/>

#### 複数行の長いコード

<CB
  lang="python"
  fileName="data_processing.py"
  content={`\
# Pythonの長いコード例
import pandas as pd
import numpy as np
from datetime import datetime, timedelta
from typing import List, Dict, Optional, Union, Tuple

def process_complex_data_analysis_with_multiple_parameters_and_long_function_name(
    input_dataframe: pd.DataFrame,
    target_columns: List[str],
    aggregation_methods: Dict[str, str],
    filter_conditions: Optional[Dict[str, Union[str, int, float]]] = None,
    date_range_start: Optional[datetime] = None,
    date_range_end: Optional[datetime] = None,
    include_statistical_summary: bool = True,
    export_to_csv: bool = False,
    output_file_path: Optional[str] = None
) -> Tuple[pd.DataFrame, Dict[str, float]]:
    """
    この関数は非常に長いパラメータリストと複雑な処理を含むデータ分析コードの例です。
    横スクロールのテストや長い行の表示テストに使用します。
    """
    
    # 長い辞書内包表記の例
    analysis_results = {
        'total_records_processed': len(input_dataframe),
        'columns_analyzed': len(target_columns),
        'average_processing_time_per_record_in_milliseconds': 0.0,
        'data_quality_score_based_on_completeness_and_accuracy': 0.0,
        'statistical_significance_p_value_for_hypothesis_testing': 0.05
    }
    
    # 長いメソッドチェーンの例
    processed_data = input_dataframe.copy().dropna().reset_index(drop=True).groupby('category').agg(aggregation_methods).round(2).sort_values(by='value', ascending=False)
    
    return processed_data, analysis_results`}
/>
